#include <iostream>

void pr(int a, char s)
{
    for (int i = 0; i < a; i++)
    {
        std::cout << s;
    }
}

int main() 
{
    int N = 5;
    
    int b = 1;

    for (int i = N; i > 0; i--)
    {
        pr(i, ' ');
        pr(b, '*');
        b += 2;
        std::cout << "\n";
    }
}
