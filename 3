#include <iostream>

int main() {
    int a;
    int sum = 0;
    int b = 1;
    std::cin >> a;
    int c;

    while (a > 0) {
        c = a % 10;
        sum += c;
        b *= c;
        a /= 10;
    }
    std::cout << "summa = " << sum << "\numnozhenie = " << b;
}
